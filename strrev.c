#include <stdio.h>
#include <string.h>
int main(void) {
  char palabra[10]="Hola";
  int len;


strrev(palabra);//invierte la cadena 

len = strlen(palabra);




printf("%s",palabra);

printf("%i\n",len );

  return 0;
}